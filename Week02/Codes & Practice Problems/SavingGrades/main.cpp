// Saving Grades Practice Problem
#include <iostream>
// Include any files needed

// Create any functions needed

using namespace std;

int main() {    // Include code to get command line arguments
// Check if enough arguments
    // If not enough enough, print an error

// Open the file provided the user

// Output the name and score of every student in the file
    // Read from the file the First and Last name of a student and output it
    // Output the score of the student

// Ask the user for a new name full name (or q to quit)
// Ask the user for the student's score

// Add to the file the new user's full name and score (make sure to follow the format provided in the file)

    return 0;
}